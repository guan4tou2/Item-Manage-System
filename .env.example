# 環境變數配置
# 複製此文件為 .env 並填寫您的設定

# 資料庫類型 (postgres 或 mongo)
DB_TYPE=postgres

# MongoDB 設定（僅在 DB_TYPE=mongo 時使用）
MONGO_URI=mongodb://localhost:27017/myDB

# PostgreSQL 設定（僅在 DB_TYPE=postgres 時使用）
POSTGRES_USER=itemman
POSTGRES_PASSWORD=itemman_pass
POSTGRES_DB=itemman
DATABASE_URL=postgresql://itemman:itemman_pass@localhost:5432/itemman

# Flask 設定
SECRET_KEY=your-secret-key-change-this-in-production
FLASK_ENV=development

# Email 通知設定
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=your-email@gmail.com
